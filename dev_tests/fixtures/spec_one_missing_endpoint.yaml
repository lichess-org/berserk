# Used with --clients-dir fake_clients. Fixture implements only /api/dev-tests/fixture.
# So /api/dev-tests/other is missing.
paths:
  /api/dev-tests/fixture:
    get: {}
  /api/dev-tests/other:
    get: {}
