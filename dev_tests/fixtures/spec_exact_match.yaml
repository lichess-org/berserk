# Used with --clients-dir fake_clients. Fixture implements GET /api/dev-tests/fixture with param "a".
# This spec matches exactly → no missing endpoints, no missing params.
paths:
  /api/dev-tests/fixture:
    get:
      parameters:
        - name: a
          in: query
